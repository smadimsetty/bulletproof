Final whole-branch review (c8ee487..pipeline/daily-cron, Daily cron): Ready to merge, with the live-trigger verification deferred to post-merge as a deliberate, platform-forced exception.

Scope reviewed: 2 commits adding the design spec/plan docs and a single new file, `.github/workflows/daily-cron.yml` (38 lines). No engine code touched -- confirmed via `git diff --stat`, only the workflow file and two `docs/superpowers/` files changed.

Why Tasks 2-3 are unverifiable pre-merge (confirmed, not just asserted): GitHub Actions only registers `workflow_dispatch`/`schedule` triggers from workflow files that exist on the repo's default branch. A workflow file living only on `pipeline/daily-cron` cannot be triggered via `gh workflow run` or fire on its cron schedule until it's on `master`. This is a real platform constraint, not a process shortcut -- live verification (trigger via `workflow_dispatch`, watch the run, confirm a real `recommendations` row lands) has to happen after this merges. Acceptable exception to this pipeline's normal verify-before-merge order, given the constraint is structural rather than a skipped step.

Static review of the workflow YAML itself:

1. Security / secrets handling -- clean.
   - The three secrets (`OURA_PERSONAL_ACCESS_TOKEN`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`) are referenced only via `${{ secrets.* }}` expansions, never hardcoded. `git log -p` and a grep for JWT-style (`eyJ...`)/Supabase (`sbp_...`) literal patterns across the branch's diff return nothing.
   - The `.env` file the workflow materializes is written to `$GITHUB_WORKSPACE/.env` on the ephemeral runner only, never committed, and discarded when the job ends. The repo's root `.gitignore` already excludes `.env` (pre-existing, confirmed at line 9), so even an accidental `git add` inside the job (which doesn't happen here) would be caught.
   - The heredoc is unquoted (`<<EOF` not `<<'EOF'`), which would normally raise a shell-injection-via-secret-value concern if the secrets were interpolated as shell variables. They aren't: GitHub Actions splices `${{ secrets.* }}` into the rendered script as literal text *before* the shell ever runs, so there's no `$VAR`-style expansion happening on the secret content at all -- the unquoted delimiter is inert here. GitHub Actions also auto-masks secret values in logs by default. Not a real risk; noting it because an unquoted heredoc piping untrusted input is a pattern worth a second look on sight, and this is why it's fine in this specific case.
   - No `permissions:` block, matching Decision 9 in the design doc: the job never touches the GitHub API (no PR comments, no contents writes), only external Oura/Supabase HTTPS calls using the secrets as bearer tokens for those third-party services. Nothing to narrow.
   - Secrets are written into a file readable by the job's own steps only, scoped to one ephemeral VM instance per run; no artifact upload, no cache step that could persist `.env` across runs.

2. Coherence with `engine/run_daily.py`'s actual behavior and exit codes -- confirmed correct.
   - Read `engine/run_daily.py`, `engine/env_loader.py`, and confirmed via grep that `recovery_repo.py`, `supabase_client.py`, `sessions_repo.py`, `rationale.py`, and `scoring.py` contain zero `try`/`except` blocks. An uncaught exception anywhere in the call chain (Oura HTTP failure, Supabase upsert failure, missing `.env`, malformed secret) propagates all the way up and exits the Python process non-zero, which GitHub Actions surfaces as a red run with no extra wiring needed -- exactly what the design spec's Goals section commits to ("non-zero exit ... already guaranteed by the engine's existing fail-loudly posture").
   - `env_loader.load_env()` unconditionally opens a literal `.env` file at the repo root and has no `os.environ` fallback (confirmed by reading the file) -- the workflow's explicit `.env`-materialization step is therefore not a defensive nice-to-have, it's required for the job to get past the first line of `main()` at all. The design doc's Background section correctly identifies this as "load-bearing," and the workflow step matches that exactly (three keys written, matching the three keys `recovery_repo`/`supabase_client` actually consume).
   - `python -m engine.run_daily` is the correct invocation given `run_daily.py`'s own `sys.path.append` fallback (added in the prior engine-productionization branch specifically to support this module-path invocation style, per that branch's final review) -- the workflow doesn't need a `cd engine &&` step or an explicit `PYTHONPATH` because of that existing fallback already being in place upstream.
   - `requires-python = ">=3.11"` in `engine/pyproject.toml` matches the workflow's `python-version: "3.11"` pin exactly. No install step is needed or present, consistent with `dependencies = []` (stdlib only) -- confirmed by reading `pyproject.toml` directly rather than trusting the design doc's claim.

3. Other things a careful reviewer would flag -- none found that block merge.
   - No `concurrency:` group: reasonable given a single daily cron plus occasional manual dispatches, and the engine's own upsert-on-`date` behavior in `recovery`/`recommendations` makes even an accidental overlapping run harmless (last writer wins on the same row, not a duplicate row or constraint violation).
   - Cron time (`0 11 * * *` UTC) has actual reasoning behind it in the design doc (pre-morning for US timezones, buffer past typical Oura sync lag, absorbs GitHub's own scheduling slack) rather than being copied unexamined from a prompt example. Trivially changed later; not a structural decision.
   - No retry/backoff beyond a manual `workflow_dispatch` re-run -- acceptable for a single-user system where a missed day just means an absent row, not a cascading failure (the engine has no `--date` backfill option to retry against, by existing design).
   - No status badge in `README.md` -- explicitly considered and rejected in the design doc's Decision 6, on the reasoning that a trigger-succeeded badge is a weaker signal than "did the row actually land," which is answered by checking Supabase directly. Reasonable to defer to the future dashboard phase.
   - The job has no test coverage of its own (by design -- this phase adds zero engine logic, only CI wiring around an already-tested command), so the only verification path left is the live trigger, correctly deferred to post-merge per the platform constraint above.

No blocking issues found in the workflow file itself. Ready to merge, with live verification (Tasks 2-3: push triggers registration, `workflow_dispatch` run, confirm a real `recommendations` row lands) to be completed immediately after this lands on `master`, as a deliberate and platform-forced exception to verify-before-merge.
