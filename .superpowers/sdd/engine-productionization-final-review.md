Final whole-branch review (613f474..pipeline/engine-productionization, Engine productionization): Ready to merge.

Scope reviewed: 13 commits covering the engine productionization design/plan docs, ported HTTP clients (env_loader.py, oura_client.py, supabase_client.py) with a 3-round worktree-compatibility fix to env_loader.py, a verbatim port of scoring.py, new sessions_repo.py and recovery_repo.py (with a positional/keyword argument convention fix), new rationale.py, and run_daily.py (with a 2-round sys.path saga: an __init__.py-level shim, critiqued as too broad, narrowed to a scoped fix inside run_daily.py itself). 21 files changed, 2421 insertions, 5 deletions, all net-new under engine/ plus two docs/superpowers files.

Checks that came back clean:
- Secrets: `git log -p` across every commit on the branch, plus a direct grep for JWT-style (`eyJ...`), Supabase (`sbp_...`), and generic `key=`/`token=`/`secret=` literal patterns, returns nothing. No `.env` file was ever added, modified, or referenced by path in any commit; `.env` is gitignored at the repo root from before this branch started and stayed untouched. The only env-related tracked file in the whole repo is the pre-existing, unrelated `apps/mobile/.env.example` (placeholder values only).
- Whole-package coherence: read all 9 production modules in `engine/` together (env_loader, oura_client, supabase_client, scoring, sessions_repo, recovery_repo, rationale, run_daily, `__init__.py`). They compose exactly as `run_daily.main()` wires them: load_env -> recovery_repo.pull_and_upsert_today (Oura -> recovery) -> sessions_repo.load_recent_history (Supabase -> history dict) -> scoring.recommend -> rationale.build_breakdown/build_internal_rationale/build_public_rationale -> supabase_client.upsert("recommendations", ...). `rationale.py`'s `_SIGNAL_LABELS` covers every value `scoring.CANDIDATES` can produce (plus forward-compatible `lower_b`/`upper_b` entries scoring doesn't emit yet, matching CLAUDE.md's still-open rotation-granularity decision rather than being dead code). `oura_client.py` and `supabase_client.py` are byte-identical to their `prototyping/weight-tuning/` originals (LF vs CRLF line endings only); `scoring.py` likewise (confirmed via newline-normalized diff) -- the "ported verbatim" claims in the design spec and README hold up under direct comparison, not just a description of intent.
- The `internal_rationale`/`public_rationale` privacy split is real, not just commented: `test_rationale.py` asserts the public string never contains the raw readiness digit while the internal string does, and this matches the actual RLS/view setup already live in `supabase/migrations/20260622001542_create_recommendations.sql` (no anon/authenticated policy on the base table; a `recommendations_public` view exposes only `date, top_pick, runner_up, public_rationale, generated_at`). This branch touches zero migration files -- confirmed via `git diff` scoped to `supabase/` returning empty -- so the schema this code assumes is exactly the schema that's actually live.
- Live end-to-end re-verification: re-ran both invocation styles named in `engine/README.md` from this worktree against the real Oura/Supabase project -- `python -m engine.run_daily` from the repo root and `cd engine && python run_daily.py` -- and both succeeded, each writing/upserting today's (2026-06-22) `recommendations` row (top_pick=mobility, runner_up=upper_a), matching Task 6's original smoke test result. `python -m pytest` in `engine/` passes 32/32 with no network access.
- `sessions_repo.py`'s known multi-row-per-date collision (Task 3's accepted limitation): confirmed at the schema level (`sessions` table has no unique constraint on `date`, unlike `recovery`/`recommendations`/`activity`, which all do) and confirmed as a faithful, intentional port -- `prototyping/weight-tuning/weight_tuning.py`'s own history-building line uses the identical dict-comprehension-keyed-by-date pattern that silently lets a later same-date row win. Still acceptable at the whole-branch level: this is the same shape of "good enough for a single-user, mostly-one-session-a-day v1" tradeoff the prototype phase already shipped and that CLAUDE.md's "rules-based engine first, tune later" philosophy explicitly sanctions; nothing in this branch makes it worse or hides it further.

The sys.path fix saga (Task 6), evaluated without per-task tunnel vision:
- Final state is genuinely good. `engine/__init__.py` is back to empty (confirmed both by `git diff` over the whole branch showing it created empty and by `wc -l` reading 0 lines now), so the rejected approach (a permanent, package-import-time, index-0 sys.path mutation affecting every consumer of `engine` including future code that imports it as a library) left no trace in the final tree.
- The accepted fix lives only in `run_daily.py`, runs once per process at module-load time, appends (not inserts) engine's own directory, and is scoped with a comment explaining exactly why (same-named-module collision risk with `prototyping/weight-tuning/`'s own `scoring.py`/`env_loader.py`, currently unrealized but real once both could plausibly run in the same interpreter). This is the correct narrow fix for what's actually a flat-import-style limitation across the whole package, not papered over.
- Verified both invocations actually exercise the fix path live (see above) rather than taking the commit message's claim on faith: `python -c` introspection confirmed `engine`'s own directory is absent from `sys.path` before `import engine.run_daily` and present (appended, not at index 0) after.
- One residual rough edge, not worth blocking on: every test file in `engine/tests/` (`test_env_loader.py`, `test_scoring.py`, `test_sessions_repo.py`, `test_recovery_repo.py`, `test_rationale.py`, `test_run_daily.py`) independently does its own `sys.path.insert(0, ...)` to find the flat-imported modules, which is a second, parallel solution to the identical problem `run_daily.py`'s fix addresses for production code. Six copies of the same path-shim line is more repetition than necessary (could live once in `engine/tests/__init__.py` or a conftest.py), but each instance is self-contained, harmless, and consistent across files -- a style nit, not a correctness or coherence problem.

Cross-task consistency check (things only visible reading the whole diff, not each task's diff alone):
- The `recovery_repo.py` keyword-vs-positional argument fix (6dadd937) reverted to match `prototyping/weight-tuning/oura_pull.py`'s established convention of always calling `upsert(..., conflict_column=...)` as a keyword. `run_daily.py`'s own call site (`supabase_client.upsert("recommendations", [row], conflict_column="date")`) independently follows the same keyword convention, so the two new call sites added in different commits stayed consistent with each other and with the prototype precedent without needing a later fix-up pass.
- `engine/README.md`'s "Running it" section describes `python -m engine.run_daily` as working "once `engine` is on `sys.path` as a package" -- written before the sys.path saga's final, narrower resolution. The phrasing is now slightly imprecise (the package doesn't need to be "on sys.path" in the traditional sense; `run_daily.py`'s own fallback handles it), but it is not wrong about the externally observable behavior: that invocation form genuinely works today, confirmed by direct re-run. Not a blocking inaccuracy, just a sentence that could be tightened in a future docs pass.
- `engine/pyproject.toml`'s `dependencies = []` is accurate end-to-end: every module across all 6 feature commits uses only `os`/`sys`/`json`/`urllib`/`datetime`, no new third-party runtime dependency was introduced despite the package growing from 3 files to 9.

No blocking issues found. Ready to merge.
