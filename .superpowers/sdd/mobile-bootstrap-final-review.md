Final whole-branch review (8635984..pipeline/mobile-bootstrap, Mobile app bootstrap): Ready to merge with fixes.

Scope reviewed: 11 commits (8ca8180..bb78b05, plus merge f9da7b3) covering authenticated RLS policies (3 revision rounds), Expo TypeScript scaffold at apps/mobile/, Supabase client wiring, Apple Sign-In wired to Supabase Auth, and EAS build/submit configuration.

Important findings (should fix before or shortly after merge):
- apps/mobile/app.json never gained the `expo-apple-authentication` config plugin entry (or `ios.usesAppleSignIn`/entitlements) when Task 6 (commit 622b0ac) added Apple Sign-In. The package and JS-level wiring (`AppleAuthentication.signInAsync`, the sign-in button, `supabase.auth.signInWithIdToken`) are correct, but without the plugin/entitlement, EAS Build's prebuild step won't apply the `com.apple.developer.applesignin` entitlement to the generated native iOS project -- `signInAsync` will fail on a real TestFlight/device build (works fine in isolation in Expo Go/JS review, which is presumably why the per-task review of 622b0ac alone didn't catch it; app.json wasn't part of that commit's diff). No README or doc anywhere in the branch flags this as a manual follow-up step either. Needs `"plugins": ["expo-apple-authentication"]` (or equivalent) added to apps/mobile/app.json before the first real EAS build is submitted for TestFlight review.

Minor findings (not blocking, worth a look in a future pass):
- eas.json's `submit.production` is `{}` (no ascApiKeyPath/ascAppId), asymmetric with the now fully-configured `submit.preview`. Pre-existing since 84678a2, not something this branch regressed -- reasonable for a bootstrap phase that's wiring up TestFlight/preview submission first, but flag before promoting a build through `production`.
- `lib/supabase.ts` uses non-null assertions (`process.env.EXPO_PUBLIC_SUPABASE_URL!`) with no runtime guard if the env vars are absent -- will throw an unhelpful "supabaseUrl is required" from the Supabase SDK rather than a clear "did you forget to set up .env" message. Low stakes for a solo-dev bootstrap.

Checks that came back clean:
- Secrets: `git log -p` on `apps/mobile/.env` and `apps/mobile/secrets/` across every commit on the branch returns nothing -- neither the real `.env` nor the `.p8` key was ever committed, even transiently. `*.p8` is gitignored both at the repo root (fd560a8) and in apps/mobile/.gitignore, and the root-level ignore landed (via the master-merge commit f9da7b3) before eas.json's `ascApiKeyPath` reference was added (84678a2/bb78b05) -- correct ordering. The only `.p8` filename appearing in the diff text is an illustrative example path/key-id inside a comment in scripts/generate_apple_oauth_secret.js, not a real path being read or a real key.
- App.tsx coherence: Task 7 (622b0ac) fully replaced Task 4's smoke-test body (the `exercises` count probe) rather than layering on top of it. No dead code, no unused imports, no leftover `StatusBar` import, and the `status` state is repurposed cleanly (now only set on sign-in error) rather than left stale.
- Migration ledger: the 3-round naming back-and-forth resolves to a truthful end state -- 20260622120000 (8ca8180) is byte-identical to what was actually first applied (quoted, space-separated policy names), confirmed against dd85aed's stated intent. 20260622130000 does a real drop-old/create-new rename to snake_case, consistent with the project's established convention (`anon_can_read_exercises` in 20260622000809_create_exercises.sql, verified to exist). Timestamps are strictly increasing and the two migrations don't collide with same-day earlier ones.
- eas.json as a whole: cli version constraint, three build profiles (development/preview/production) and the submit.preview.ios block (ascAppId + the three ASC API key fields) are internally consistent and match the secrets/ASC key referenced. No conflicting or orphaned fields.
- Merge commit f9da7b3 (master into branch) is clean -- no conflict markers anywhere in the tree, and the *.p8 gitignore entries at root vs. apps/mobile level are legitimate defense-in-depth (per fd560a8's own commit message), not accidental duplication.
- apps/mobile/.env.example contains only placeholder values (`your-project-ref`, `your-anon-key-here`), no real project ref or key.

Fix needed before merge is safe to call complete: add the `expo-apple-authentication` plugin entry to apps/mobile/app.json. This is a one-line config fix, not a redesign -- doesn't need to block the merge of this bootstrap branch itself if Sohan wants to land the scaffold now and fix prebuild config in a fast follow-up, but it must land before anyone runs an actual EAS build intended for a real device/TestFlight Apple Sign-In test, or that test will fail mysteriously.
