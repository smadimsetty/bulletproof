// apps/web/app/page.tsx
//
// Placeholder route segment -- Task 1's scope is the app scaffold (layout,
// static export config) only. Next.js App Router's static export needs at
// least one page.tsx under a route segment to emit any HTML, so this
// trivial placeholder exists purely to make `next build` produce
// out/index.html. Task 4 replaces this with the real recommendation/summary
// UI; nothing here should be treated as final.
export default function Page() {
  return <main>Bulletproof</main>;
}
